{
  "name": "Total-Suspender-webextension",
  "version": "2.0.0",
  "description": "A minimalistic web extension for firefox that suspends natively (with tabs.discard) not-active tabs.",
  "scripts": {
    "test": "web-ext lint --config .web-ext-config.js",
    "build": "web-ext build --config .web-ext-config.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.13.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.16.0"
  }
}
